## Authentication with firebase by email and social auth.


I have used nodejs for linkedin authentication which is not providing firebase.

### Install dependencies and run the app
```
cd react
npm install
npm start
```

```
cd backend
npm install
npm start
```